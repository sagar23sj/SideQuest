import Foundation

// MARK: - Generated_Models (placeholder)
//
// This folder will hold the Swift `Generated_Models` authored/generated from
// the shared OpenAPI 3 contract (`backend/api/openapi.yaml`): `ActionItem`,
// `Bucket`, `ActionPlan`, `SubAction`, `Account`, `Thought`, `SyncMeta`,
// `TaskReminder`, `TimeOfDay`, and the `ActionStatus` / `ContentType` /
// `Timeframe` enums — all `Codable`, with on-the-wire JSON identical to the
// Android client and the backend.
//
// Implemented by task 2 ("Define data models from the OpenAPI schema").
// This placeholder establishes the folder in the shared module so both the
// main app and the Share Extension link the same model types.

/// Marker namespace for the shared model layer. Replaced by the real
/// `Generated_Models` in task 2.
public enum Models {}
